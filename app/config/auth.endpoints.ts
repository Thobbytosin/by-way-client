import { API_AUTH_PATHS } from "./api.routes";

////////////////////////////////// USER ENDPOINTS
const LOGIN = `${API_AUTH_PATHS.LOGIN}`;
const SOCIALLOGIN = `${API_AUTH_PATHS.SOCIALLOGIN}`;
const LOGOUT = `${API_AUTH_PATHS.LOGOUT}`;
const SIGNUP = `${API_AUTH_PATHS.SIGNUP}`;

export { LOGIN, SOCIALLOGIN, LOGOUT, SIGNUP };
